# Environment Variables for Vercel Deployment
# ================================================
# IMPORTANT: Copy values from backend/.env file
# Then paste to Vercel Dashboard -> Settings -> Environment Variables
# ================================================

# Server Configuration
NODE_ENV=production
PORT=3001

# Firebase Configuration
# ================================================
# Copy the entire FIREBASE_SERVICE_ACCOUNT value from backend/.env
# It should be a SINGLE LINE JSON string
FIREBASE_SERVICE_ACCOUNT=<COPY_FROM_backend/.env>

# Copy from backend/.env
FIREBASE_STORAGE_BUCKET=<COPY_FROM_backend/.env>

# Copy from backend/.env (optional)
FIREBASE_DATABASE_URL=<COPY_FROM_backend/.env>

# OpenAI API Key
# ================================================
# Copy from backend/.env
OPENAI_API_KEY=<COPY_FROM_backend/.env>

# Google Cloud API Key
# ================================================
# Copy from backend/.env
GOOGLE_API_KEY=<COPY_FROM_backend/.env>

# CORS Configuration
CORS_ORIGIN=*

# ================================================
# HOW TO USE:
# 1. Open backend/.env file on your computer
# 2. Copy each value from there
# 3. Paste to Vercel Dashboard:
#    - Go to vercel.com
#    - Select your project
#    - Settings -> Environment Variables
#    - Add each variable with the value from .env
# 4. Make sure FIREBASE_SERVICE_ACCOUNT is ONE LINE
# 5. Select "Production", "Preview", "Development" for each
# 6. Click "Save"
# 7. Redeploy your project
# ================================================
